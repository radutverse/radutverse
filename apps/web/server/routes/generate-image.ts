import { RequestHandler } from "express";

export const generateImage: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const editImage: RequestHandler = (req, res) => {
  res.json({ ok: true });
};
