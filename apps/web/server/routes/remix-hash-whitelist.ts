import { RequestHandler } from "express";

export const handleAddRemixHash: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const handleCheckRemixHash: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const handleGetRemixHashes: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const handleClearRemixHashes: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const handleGetRemixHashesFull: RequestHandler = (req, res) => {
  res.json({ ok: true });
};

export const handleDeleteRemixHash: RequestHandler = (req, res) => {
  res.json({ ok: true });
};
